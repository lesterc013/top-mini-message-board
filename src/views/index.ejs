<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
</head>
<body>
  <!-- Given messages array, display each of the properites -->
  <% messages.forEach((message) => { %>
    <!-- <div>
      <h3><%= message.user %></h3>
      <p> <%= message.text %> </p>
      <p> <%= message.added %> </p>
    </div> -->
    <%- include('message', {message}) %>
  <% }) %>
</body>
</html>